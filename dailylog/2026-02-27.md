day 10, Feb 27
- jupyter notebook pandas practice
	- trouble with dtypes need to check that data set makes sense not having str for years etc
	- jupyter notebook quirk earlier vars and functions are saved and can be used in later cells but need to be run first, esp after just opening a file 
	- mainly sql queries written in pandas, a bit confusing how there is python dictionary syntax and pandas syntax from tutorial but makes sense
- jupyter notebook data cleaning with titanic data set
	- 1: setting up titanic dataset
	- 2: data inspection
		- shows missing data, number of nulls in each column
		- datatypes make sense for each category
	- 3: Handle missing values
		- removed columns with missing age
	- 4: Handle outliers
		- matplotlib of a column - fare - which showed graph of fare cost and you can see outliers
		- removed outliers by removing columns with fares over outlier threashold
	- 5: Feature engineering
		- trouble with missing column names, no names makes it all pointless, skipped
	- 6: Remove duplicates & finalize
		- removing duplicates and renaming to easier to understand column names